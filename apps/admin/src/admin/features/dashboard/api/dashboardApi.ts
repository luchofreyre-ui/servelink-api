/**
 * Dashboard summary. When backend adds GET /admin/dashboard/summary, switch to it.
 * Until then the page composes from useDispatchExceptions, useOpsAnomalyCounts, useActivityList, useActiveDispatchConfig.
 */
export type { AdminDashboardSummary } from "./types";
