/** Mega Phase A — operational replay intelligence suite (observe / compare / interpret only). */

export const OPERATIONAL_REPLAY_INTELLIGENCE_SUITE_ENGINE_VERSION =
  "operational_replay_intelligence_suite_mega_a_v1" as const;

export const OPERATIONAL_REPLAY_PAIRING_CATEGORY = {
  CONSECUTIVE_REFRESH_LINEAGE_V1:
    "consecutive_refresh_lineage_pairing_v1",
  EXPLICIT_ADMIN_SELECTION_V1:
    "explicit_admin_selection_pairing_v1",
} as const;
